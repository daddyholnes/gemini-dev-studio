// Gemini Developer Studio - Config
window.GEMINI_CONFIG = {
  // Placeholder for Gemini API key (should be securely injected in production)
  GEMINI_API_KEY: '',
  // Gemini model to use (for future expansion)
  GEMINI_MODEL: 'gemini-1.5-pro-latest',
  // API endpoint (can be local Flask backend or direct Gemini endpoint)
  API_ENDPOINT: 'http://localhost:5000/api/gemini',
  // App version
  VERSION: '0.1.0',
};
